---
import '@/styles/global.css'
import '@fontsource/babylonica'
import '@fontsource/licorice'
import BgPhotos from '@/components/bg-photos.astro'

export interface Props {
  title: string
}

const { title } = Astro.props
---

<!doctype html>
<html lang='es'>
  <head>
    <meta charset='UTF-8' />
    <meta name='description' content='Invitación de boda' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <link rel='icon' type='image/svg+xml' href='/favicon.svg' />
    <meta name='generator' content={Astro.generator} />
    <title>{title}</title>
    <style>
      @import '../styles/global.css';
    </style>
  </head>
  <body class='bg-white min-h-screen relative overflow-x-hidden'>
    <BgPhotos />

    <main class='relative z-10 min-h-screen py-10'>
      <div class='mx-auto w-full max-w-sm'>
        <div
          class='bg-white/90 backdrop-blur-sm rounded-2xl shadow-2xl overflow-hidden'
        >
          <slot />
        </div>
      </div>
    </main>
  </body>
</html>
